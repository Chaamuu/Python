count=0
def find_length(num):
    if num>0:
        global count
        count=count+count+1
        find_length(num//10)
        
find_length(467)
print(count)
##
def print_num(num):
    if num<10:
        print_num(num+1)
    print(num)
    
print_num(1)
